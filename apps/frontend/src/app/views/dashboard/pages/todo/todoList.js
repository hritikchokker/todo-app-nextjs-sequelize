import React, { useEffect } from 'react';
function TodoList() {
  useEffect(() => {
    // hit the api here
    // example dispatch(getTodoList())
  }, []);

  return <div>TodoList</div>;
}

export default TodoList;
